// @flow

const defaults = {
  currentDrawing: {
    __typename: 'CurrentDrawing',
    name: null,
    root: null,
  },
  dialogs: {
    __typename: 'Dialogs',
    openFile: false,
  },
};

export default defaults;
